class Solution(object):
    def minSteps(self, s, t):
        """
        :type s: str
        :type t: str
        :rtype: int
        """
        steps = 0
        for c in set(s):
            steps += max(0, s.count(c) - t.count(c))
        return steps
